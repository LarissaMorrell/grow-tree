import React from "react";
import { shallow, mount } from "enzyme";
import ResourcesCounter from "../components/ResourcesCounter";

describe("<ResourcesCounter />", () => {
  it("renders without crashing", () => {
    // shallow(<ResourcesCounter />);
  });
});
